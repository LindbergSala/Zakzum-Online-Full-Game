# PostgreSQL connection string for local development and Vercel.
# Replace the placeholder values with a real database connection string.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"
